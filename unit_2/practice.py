# type('')  - takes an object and returns the data type
#.upper() needs no input
#.replace(oldstr,newstr)
#str() -typecast function
#float(object) type cast a float
#  x**y exponentiation
#x//y  floor division always rounds down to nearest integer
# print("f" * 10)  returns 10 f's
#print (x==y) result becomes a boolean

# %=2 reassignment opperator
#  ** = 3  x
#not flips the boolean
#boolean  of  empty is false
#letters = letters.replace('A','Z'A)
#ord(
#  )returns aski number
#colors.insert(1, "cyan")
#colors.extend['fsf','fjk']
#letters = list(letters)  converts sequence to list
#bool()
#print(string.isnumeric()) returns flse if not all numeric
#print ( 'B' < 'b')
#colors.pop() 
#print(range(10))
#print(list(range(10)))
#.insert(index, intem)
#fruits.sort(reverse=True)
#letters_list = letters.split('-')
#.sort() sorts list in ascending order
#.index(item) returns first occurance
#.remove(item) removes first occurance
#for x in range() loop through a certain number of items
#random.choice - returns a random choice from a sequence
#color = color.pop(1)  1 is your things ind
# kex will store pop value in color
#import string
#use .extend
#continue skips the rest of this iteration
#break ends the loop
#print all_characters   is letters + digits + punctuation
#letters = string.ascii_letters
#digits = string.digits
#else/break/continue
#string.punctuation
# new_string = ','.join(letters_list)

b = [1,2,3]
c = [1,2,3]

b.append(c)

print(b)

print(range(10).append(3))